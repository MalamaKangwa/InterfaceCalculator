using System;
using System.Collections.Generic;
using System.Text;

namespace Helpers
{
    class Rounding
    {
        public static dynamic RoundTo2(dynamic a)
        {
            return Math.Round(a, 2);
        }
    }
}
